import { LINKED_LIST } from './_2linked_list';
import { STACK } from './_3stack';
import { QUEUE } from './_4queue';
import { HEAP } from './_5heap';
import { UNION_FIND } from './_6union_find';
import { TREE } from './_7tree';

export const DATA_STRUCTURES = {
  LINKED_LIST,
  STACK,
  QUEUE,
  HEAP,
  UNION_FIND,
  TREE,
};
